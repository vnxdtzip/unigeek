# README images

Drop the images referenced by the root `README.md` here:

| File | Used for |
|------|----------|
| `logo.svg` | Top logo (from unigeek.xid.run/favicon.svg) |
| `main-menu.png` | Main Menu screenshot |
| `character.png` | Agent / Character screen screenshot |
| `subghz-waterfall.png` | Sub-GHz Waterfall screenshot |

Screenshots are 16:9 captures from the Screen Mirror (`/app/remote`). Keep them as PNG
and reasonably small (the README renders them ~280 px wide) so the page loads fast on GitHub.
